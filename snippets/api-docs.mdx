export const apiWarning = '<Warning>These steps will only work if the PRC API pack has been purchased for the server. If it has not, it needs to be purchased before they continue with the steps.</Warning>';
